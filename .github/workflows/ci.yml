name: Premier test de Workflow CI

on:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: unbutu-latest
        steps:
        - uses: actions/checkout@v2
        - name: Exécution d'un script simple
          run: echo "Le workflow CI est déclenché par un push sur GitHub, une pull request ou encore tous les jours à 2h00 UTC"